'use strict'

describe('Sexy Ultimate test(had to do the directives this way)',function(){
  //load module - this is default
  beforeEach(module('regUstratorApp'));
  beforeEach(module('drawObjs'));
  /*
    init ObjFactory

  */
});