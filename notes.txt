1. text on things:
	does CSS work?
	figure out sizing
	
2. images on things
3. animations 
4. Dev env
	a. tests
	#b. camera toggle 
5. tests
  make test objprops objects?? idk how


inner text:
//need to use text geometry

.directive('text', function (sceneData) {
    return {
      template: '<div ng-transclude></div>',
      restrict: 'E',
      transclude: true,
      replace: true,
      link: function postLink(scope, element, attrs) {
        // create a canvas element
        var innerText = element.html().split(/[><]/)[2];//so bad


	